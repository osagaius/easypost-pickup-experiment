# easypost-pickup-experiment

Quick proof of concept using the EasyPost API. It creates a shipment and schedules a pickup.


To use:
Get an API key
Replace the API_KEY variable your test API key
run 'npm install'
run 'node pickup.js'
